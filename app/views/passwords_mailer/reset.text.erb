Reset your password

Use the link below to choose a new password for the Deccan Queen on Rails admin.

<%= edit_password_url(@admin_user.password_reset_token) %>

This link expires in <%= distance_of_time_in_words(0, @admin_user.password_reset_token_expires_in) %>.
If you did not ask for a reset, ignore this email and nothing will change.
